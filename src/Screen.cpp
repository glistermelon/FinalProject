#include "../include/Screen.h"

void Screen::update() {
    // Move blocks based on velocity
    for (Block* block : blocks) {
        
    }

    // Handle Collisions
    
    // Add gravity
    for (Block* block : blocks) {
        
    }
}